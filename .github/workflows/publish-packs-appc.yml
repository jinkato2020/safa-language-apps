name: Publish App C packs (音源マスター→32k→Release)

# 手動専用。Git管理の音源マスター 聞いて話せる英語/audio/<lang>/{conv,gram}(en/ja) を
# 唯一の正として、32k化→STORED zip→Release packs-appc に公開する。
# scripts/build_appc_packs_32k.py を --publish で実行(catalog更新+audioVersion+1+upload)。

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'true=ビルドのみ(公開しない) / false=公開まで'
        required: true
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v5

      - name: Install ffmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build (+publish) packs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          SCRIPT="聞いて話せる英語/scripts/build_appc_packs_32k.py"
          if [ "${{ github.event.inputs.dry_run }}" = "false" ]; then
            python "$SCRIPT" --publish | tee -a "$GITHUB_STEP_SUMMARY"
          else
            python "$SCRIPT" | tee -a "$GITHUB_STEP_SUMMARY"
            echo "::notice::dry_run=true のためビルドのみ(公開なし)。公開は dry_run=false で再実行。"
          fi
