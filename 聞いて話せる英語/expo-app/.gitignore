# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env.*
!.env.example

# EAS / Apple credentials (NEVER commit these to a public repo)
credentials.json
credentials/
*.p12
*.p8
*.mobileprovision
*.cer
*.key

# typescript
*.tsbuildinfo

# 言語パック書き出し (scripts/export-packs.mjs の生成物。Release にアップロードする)
dist-packs/

# backup files generated by import scripts
*.backup-*

# generated native folders
/ios
/android

# Listening mode concatenated audio (Android 起動不能のため v1.5.14 でロールバック)
# scripts/generate-listening-audio.mjs で再生成可能
assets/audio/listening/
data/listeningMetadata.json
